import org.gradle.internal.os.OperatingSystem

apply plugin : 'org.ysb33r.doxygen'

doxygen {
    executables {
        doxygen file('fake-doxygen' + OperatingSystem.current().isWindows() ? '.bat' : '.sh')
    }
}